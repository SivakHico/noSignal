export default function Power() {
    let power = function () {
        let body = document.getElementById("root");
        body.class = (body.class == "on") ? "off" : "on";
    }

    return (  );
}



